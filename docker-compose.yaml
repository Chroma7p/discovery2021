version: '3.1'

services: 

  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: hack2021tofu
    ports:
      - 27017:27017
    volumes:
      - ./db:/data/db
      - ./configdb:/data/configdb

  backend:
    build: ./backend
    ports:
      - 8000:8000
    environment:
      - PORT=8000

  front:
    build: ./front
    ports:
      - 80:80
    